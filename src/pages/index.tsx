import Button from "@/components/Button";
import Layout from "@/components/Layout";
import useLoadList from "@/helpers/hooks/useLoadList";
import type { NextPage } from "next";
import Head from "next/head";
import Image from "next/image";
import Link from "next/link";

const Home: NextPage = () => {
  // const { showModal, closeModal } = useModal()

  const { data, loading } = useLoadList();

  console.log(data, loading);
  return (
    <Layout>
      <div className="bg-gray-800 h-full w-full min-h-screen p-6">
        {/* <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head> */}

        <div className="text-8xl text-white font-semibold">
          Atomixy - The next wiki
        </div>
        <br />
        <br />
        <div className="w-full place-items-center items-center">
          <Link href={`/flow/create`}>
            <Button>Create Flow</Button>
          </Link>
        </div>

        {/* IMAGES OF FLOW examples */}
        <br />
        <br />
        <br />
        <div className=" grid grid-cols-2 gap-2">
          <div className="bg-purple-300"></div>
          <div className=" max-h-96 overflow-scroll">
            {!!data?.length &&
              data.map((item, i) => <List key={i} {...item} index={i} />)}
          </div>
        </div>
      </div>
    </Layout>
  );
};

export default Home;

// #414a4c	(65,74,76)
// #3b444b	(59,68,75)
// #353839	(53,56,57)
// #232b2b

const List = (props) => {
  return (
    <Link
      //send id ipfs, query -> network
      href={`/flow/${props?.ipfsPath}?key=${props.index}`}
    >
      <div className="p-4 mb-4 mx-1 bg-white rounded-md  cursor-pointer">
        <div className="text-gray-700 font-semibold ">{props?.title}</div>
        <div className="text-gray-400 text-sm ">{props.description}</div>
        <div>Created by</div>
        <div>Created at: {props?.createdAt}</div>

        <div>tags tags tags</div>
        <div>Networks: {props?.chainId}</div>
        <div>Pools</div>
      </div>
    </Link>
  );
};
